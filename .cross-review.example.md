<!--
  .cross-review.example.md
  ==========================================================================
  使い方：
  1. このファイルをリポジトリ直下に `.cross-review.md` という名前でコピーする。
  2. 下の <...> プレースホルダを、自分のプロジェクトで壊れやすい注意点に置き換える。
  3. 汎用部分（次の 3 ファイル）をそのままコピーして取り込む：
       tools/cross-review.js / docs/cross-review.md / .cross-review.example.md
     テスト tests/cross-review.test.js (vitest) は任意。取り込み先が vitest を使うときだけ、
     require パスを合わせて同梱する (同梱しなくても engine の振る舞いは upstream のテストが担保)。
     tools/*.js は CommonJS なので、ルート package.json が "type":"module" の場合は
     tools/package.json に "type":"commonjs" を置く。
  4. ルート package.json の scripts に追加：
       "review:codex":     "node tools/cross-review.js codex",
       "review:codex:fix": "node tools/cross-review.js codex --fix",
       "review:claude":    "node tools/cross-review.js claude"
  5. codex / claude のスタンドアロン CLI を PATH に通す (拡張/アプリとは別物)。
     クラウド実行などで CLI を起動できない環境では、`node tools/cross-review.js subagent`
     が CLI 無しでレビュー用プロンプトを stdout に出すので、それを Claude の客観サブエージェントへ渡す。

  観点を探す順 (loadChecklist)：
    環境変数 CROSS_REVIEW_CHECKLIST(パス) → <cwd>/.cross-review.md
    → <スクリプト>/../.cross-review.md → 組み込みの汎用観点
  どれも無ければ汎用観点でレビューします (起動時に stderr へ警告)。
  スクリプトの位置も探すので、cwd がリポジトリ直下でなくても
  (絶対パスや ../tools/cross-review.js などで起動しても) 観点を見つけられます。

  注意：この .md の全文が、そのままレビュアーへの「観点プロンプト」になります。
  この HTML コメントは .cross-review.md へコピーした後で削除して構いません
  (残っていても害はありませんが、観点本文だけにしておくのが綺麗です)。
-->

あなたは <プロジェクト名 / 一言説明> のコードレビュアーです。  
以下の差分を、このリポジトリ固有の観点でレビューしてください。  
日本語で、各指摘に重大度 (blocker / 要修正 / 提案) を付けてください。  
問題が無ければ、その旨も明記してください。

重点的に見る観点：
- <壊れやすい注意点、禁止事項をここに列挙する>
  例) 永続化フォーマットの後方互換性 / 特定 API の使用禁止 / 命名、配置の規約 /
      バージョン番号やキャッシュ名の同期 / セキュリティ上の禁止事項 など
- <観点2>
- <観点3>
- 構文/整合性：<このリポの lint / test / ビルド コマンド> を通る変更か。

該当しない観点は飛ばして構いません。  
差分に現れていない既存の動作が壊れていないか (回帰) も疑ってください。
